task: <name>
context: <orchestrator provides this>  # brief summary + critical paths
files: ["src/components/..."]
test: "vitest <name> && playwright --grep <name>"
max_tries: 3
return: { done, files, tests, next }


